{
  "name": "david-novotny-portfolio",
  "version": "1.0.0",
  "description": "Professional portfolio website for David Novotny",
  "author": "David Novotny",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "live-server src --port=3000 --open=/index.html",
    "start": "npm run dev",
    "build": "npm run build:css && npm run build:copy",
    "build:css": "cp -r src/css dist/css",
    "build:copy": "mkdir -p dist && cp -r src/index.html src/js src/assets dist/ 2>/dev/null || true",
    "preview": "live-server dist --port=8080",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
